# 코덱스 전수 감사 요청: 전략 아카이브 시스템

## 배경

김과외 로드맵 사이트(Next.js SSG)에 전략 아카이브 시스템을 구축했습니다. 기존 단일 md 파일(132KB, 1869줄)에서 다중 md + 자동 생성 인덱스 JSON 2계층 구조로 전환한 Phase 1이 완료된 상태입니다.

이 시스템의 정보 구조, 메타데이터 스키마, 인덱스 생성 로직, UI 구현, 코워크 스킬 설계를 전수 감사해 주십시오.

---

## 질문 1: 정보 구조(IA) 평가

현재 디렉토리 구조와 문서 타입 분류를 평가해 주십시오.

```
src/content/archive/
├── documents/
│   ├── roadmap/2026/          (4개 — 3~6월 로드맵)
│   ├── roadmap-history/2026/  (0개 — 빈 디렉토리)
│   ├── codex-prompt/2026/     (9개 — v6 6개 + v9.1 3개)
│   ├── codex-review/2026/     (7개 — 자문위원 1~6 + 교차분석)
│   ├── data-analysis/2026/    (1개 — 안심결제 갭분석)
│   ├── audit/2026/            (1개 — 아카이브 전략 감사)
│   └── sop/2026/              (0개 — 빈 디렉토리)
├── legacy/2026/               (1개 — 이관 전 원본 스냅샷)
├── index.generated.json       (22개 문서 메타데이터)
└── tags.generated.json        (40개 태그 → 문서 매핑)
```

**문서 타입 8개:**

| type | 현재 문서 수 | 설명 |
|------|------------|------|
| roadmap | 4 | 월별 전략 로드맵 (canonical 최신본) |
| roadmap-history | 0 | 로드맵 과거 버전 |
| codex-prompt | 9 | Codex에 보낸 검토 요청 원문 |
| codex-review | 7 | Codex 자문 응답 결과 |
| data-analysis | 1 | 갭분석, KPI 분석 |
| audit | 1 | 시스템/프로세스 감사 |
| sop | 0 | 운영 절차서 |
| legacy-snapshot | 0 | 이관 전 원본 스냅샷 |

점검 요청:
- 타입 분류가 적절한가? 빠진 타입이나 불필요한 타입이 있는가?
- 빈 디렉토리(roadmap-history, sop)를 미리 만들어둔 것이 적절한가?
- `{type}/{year}/{date}-{slug}.md` 네이밍 컨벤션에 문제가 있는가?
- 22개 문서가 현재 분류에 올바르게 배치되었는가?
- Phase 2에서 추가할 문서(로드맵 v1~v9 이력, SOP 4세트 등)를 고려할 때 확장성이 충분한가?

---

## 질문 2: 메타데이터 스키마 감사

각 md 파일의 YAML frontmatter 스키마입니다:

```yaml
---
id: codex-review-2026-04-07-01-ai-product-strategy
title: "AI 프로덕트 전략가 관점 — 에이전트 시대 대비"
date: 2026-04-07
type: codex-review
artifact_kind: review
status: final
version: v1
canonical: true
period: cross
tags:
  - topic/ai-agent
  - topic/data-moat
  - metric/revenue
  - method/codex
summary_line: "AI 시대에 사라지는 것은 검색창의 희소성..."
key_findings:
  - "대체불가능 자산 3가지: 성과 데이터 그래프, 신뢰·정산 레일, 관계 운영 OS"
  - "AI 에이전트 매칭 v2는 연간 6.5~9.7억원 매출 uplift 잠재력"
  - "올해 반드시 완성할 것: 교육 OS v1"
related_ids:
  - codex-review-2026-04-07-02-unit-economics
source_paths:
  - /Users/lilith/roadmap-site/src/content/archive/legacy/...
source_type: markdown
token_hint: medium
---
```

**필드 목록:**

| 필드 | 타입 | 필수 | 용도 |
|------|------|------|------|
| id | string | Y | 고유 식별자, URL slug로 사용 |
| title | string | Y | 한글 제목 |
| date | string (YYYY-MM-DD) | Y | 문서 작성일/해당 기간 시작일 |
| type | enum | Y | 8개 타입 중 하나 |
| artifact_kind | enum | Y | report/prompt/sop/snapshot/review |
| status | enum | Y | draft/final |
| version | string | Y | v1, v2, v6, v6b, v9.1 등 |
| canonical | boolean | Y | 같은 주제의 최신본 여부 |
| period | string | N | mar/apr/may/jun/cross 등 |
| tags | string[] | Y | 접두사 배열 (topic/metric/method/period/artifact) |
| summary_line | string | Y | 1줄 요약 |
| key_findings | string[] | Y | 핵심 발견 3개 이내 |
| related_ids | string[] | N | 관련 문서 id 배열 |
| source_paths | string[] | N | 원본 파일 경로 (참고용) |
| source_type | enum | N | manual/codex/docx-convert/markdown |
| token_hint | enum | Y | small(<2K)/medium(2-8K)/large(8K+) |

점검 요청:
- 스키마에 빠진 필드나 불필요한 필드가 있는가?
- `artifact_kind`와 `type`의 역할이 중복되는가?
- `version` 필드의 값이 통일되지 않았다 (v1, v6, v6b, v9.1). 정규화 방안은?
- `canonical` 관리가 수동인데, 자동 판별 로직이 필요한가?
- `period` 값이 불규칙하다 (mar, may, cross). 표준화가 필요한가?
- `tags` 접두사 5개(topic/metric/method/period/artifact)가 충분한가?
- `related_ids`의 양방향 정합성을 어떻게 보장하는가? (A→B 참조 시 B→A도 있어야 하는가?)

---

## 질문 3: 인덱스 생성 스크립트 코드 리뷰

`scripts/rebuild-index.mjs` — frontmatter를 파싱하여 index.generated.json과 tags.generated.json을 생성합니다:

```javascript
#!/usr/bin/env node
import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const ARCHIVE_DIR = path.join(__dirname, '..', 'src', 'content', 'archive');
const DOCS_DIR = path.join(ARCHIVE_DIR, 'documents');

function parseFrontmatter(content) {
  const match = content.match(/^---\n([\s\S]*?)\n---/);
  if (!match) return null;
  const fm = {};
  let currentKey = null;
  let currentArray = null;

  for (const line of match[1].split('\n')) {
    const keyOnlyMatch = line.match(/^(\w[\w_]*)\s*:\s*$/);
    const kvMatch = line.match(/^(\w[\w_]*)\s*:\s*(.+)$/);
    const arrayItemMatch = line.match(/^\s+-\s+(.+)$/);

    if (keyOnlyMatch) {
      if (currentKey && currentArray) fm[currentKey] = currentArray;
      currentKey = keyOnlyMatch[1];
      currentArray = [];
    } else if (kvMatch) {
      if (currentKey && currentArray) fm[currentKey] = currentArray;
      currentKey = kvMatch[1];
      let val = kvMatch[2].trim();
      if (val === '[]') { currentArray = []; }
      else {
        currentArray = null;
        val = val.replace(/^["']|["']$/g, '');
        if (val === 'true') val = true;
        else if (val === 'false') val = false;
        fm[currentKey] = val;
      }
    } else if (arrayItemMatch && currentKey) {
      if (!currentArray) currentArray = [];
      let val = arrayItemMatch[1].trim().replace(/^["']|["']$/g, '');
      currentArray.push(val);
    }
  }
  if (currentKey && currentArray) fm[currentKey] = currentArray;
  return fm;
}

function walkDocs(dir) {
  const results = [];
  if (!fs.existsSync(dir)) return results;
  for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
    const fullPath = path.join(dir, entry.name);
    if (entry.isDirectory()) results.push(...walkDocs(fullPath));
    else if (entry.name.endsWith('.md')) results.push(fullPath);
  }
  return results;
}

const files = walkDocs(DOCS_DIR);
const index = [];
const tagsMap = {};

for (const filePath of files) {
  const content = fs.readFileSync(filePath, 'utf-8');
  const fm = parseFrontmatter(content);
  if (!fm || !fm.id) { console.warn(`Skipping: ${filePath}`); continue; }
  const relPath = 'src/content/archive/' + path.relative(ARCHIVE_DIR, filePath);
  const entry = {
    id: fm.id, title: fm.title || '(untitled)', date: fm.date || '2026-01-01',
    type: fm.type || 'unknown', artifact_kind: fm.artifact_kind || '',
    status: fm.status || 'final', version: fm.version || 'v1',
    canonical: fm.canonical === true || fm.canonical === 'true',
    period: fm.period || 'cross', tags: fm.tags || [],
    summary_line: fm.summary_line || '', key_findings: fm.key_findings || [],
    related_ids: fm.related_ids || [], path: relPath,
    token_hint: fm.token_hint || 'medium',
  };
  index.push(entry);
  for (const tag of entry.tags) {
    if (!tagsMap[tag]) tagsMap[tag] = [];
    tagsMap[tag].push(entry.id);
  }
}

index.sort((a, b) => b.date.localeCompare(a.date));
fs.writeFileSync(path.join(ARCHIVE_DIR, 'index.generated.json'),
  JSON.stringify(index, null, 2), 'utf-8');
const sortedTags = Object.fromEntries(
  Object.entries(tagsMap).sort(([a], [b]) => a.localeCompare(b)));
fs.writeFileSync(path.join(ARCHIVE_DIR, 'tags.generated.json'),
  JSON.stringify(sortedTags, null, 2), 'utf-8');
```

점검 요청:
- 커스텀 YAML 파서가 아닌 정식 YAML 라이브러리(gray-matter 등)를 써야 하는가?
- 현재 파서가 처리하지 못하는 엣지 케이스가 있는가? (중첩 객체, 멀티라인 문자열, 특수문자 등)
- 인덱스에 본문 텍스트를 포함하지 않는 설계가 적절한가? (검색 기능 추가 시 영향)
- `tags.generated.json`의 구조가 효율적인가? 역인덱스(문서→태그)도 필요한가?
- 빌드 파이프라인(npm run build) 전에 자동으로 rebuild-index를 실행하는 것이 나은가?
- id 중복 검사가 없다. 추가해야 하는가?

---

## 질문 4: UI 구현 감사

### 4-1. 아카이브 라이브러리 (`src/lib/archive.ts`)

```typescript
import fs from 'fs';
import path from 'path';

export interface ArchiveEntry {
  id: string; title: string; date: string; type: string;
  artifact_kind: string; status: string; version: string;
  canonical: boolean; period: string; tags: string[];
  summary_line: string; key_findings: string[];
  related_ids: string[]; path: string; token_hint: string;
}

const ARCHIVE_DIR = path.join(process.cwd(), 'src', 'content', 'archive');

export function getArchiveIndex(): ArchiveEntry[] {
  const indexPath = path.join(ARCHIVE_DIR, 'index.generated.json');
  if (!fs.existsSync(indexPath)) return [];
  return JSON.parse(fs.readFileSync(indexPath, 'utf-8'));
}

export function getArchiveDoc(id: string): { meta: ArchiveEntry; content: string } | null {
  const index = getArchiveIndex();
  const meta = index.find((e) => e.id === id);
  if (!meta) return null;
  const filePath = path.join(process.cwd(), meta.path);
  if (!fs.existsSync(filePath)) return null;
  const raw = fs.readFileSync(filePath, 'utf-8');
  const content = raw.replace(/^---[\s\S]*?---\n*/, '');
  return { meta, content };
}

export function getArchiveTags(): Record<string, string[]> {
  const tagsPath = path.join(ARCHIVE_DIR, 'tags.generated.json');
  if (!fs.existsSync(tagsPath)) return {};
  return JSON.parse(fs.readFileSync(tagsPath, 'utf-8'));
}

export function getTypeLabel(type: string): string { /* 8개 타입 → 한글명 매핑 */ }
export function getTypeColor(type: string): string { /* 타입 → Tailwind 색상 클래스 */ }
```

### 4-2. 라우팅 구조

| 경로 | 렌더링 | 데이터 소스 |
|------|--------|------------|
| `/archive` | SSG 목록 | index.generated.json |
| `/archive/[id]` | SSG 상세 (22개 정적 경로) | index.json + md 파일 본문 |
| `/archive/legacy` | SSG | legacy/2026/2026-04-07-flat-archive.md |

### 4-3. 목록 페이지 (`/archive`)
- 타입별 그룹핑 + 문서 수 통계 카드
- 각 문서: 날짜, 버전, canonical 상태, 태그 3개, summary_line 표시
- 레거시 아카이브 링크 하단 배치

### 4-4. 상세 페이지 (`/archive/[id]`)
- `generateStaticParams()`로 22개 정적 경로 생성
- 메타 정보 바: 날짜, 버전, canonical 상태, token_hint, 전체 태그
- 관련 문서 링크 (related_ids 기반)
- ArchiveViewer 컴포넌트로 마크다운 렌더링

점검 요청:
- SSG에서 fs 직접 사용이 Next.js 베스트 프랙티스에 부합하는가?
- `getArchiveIndex()`가 매번 JSON을 파싱하는데, 캐싱이 필요한가?
- `getArchiveDoc()`에서 frontmatter 제거 정규식이 엣지 케이스에 안전한가?
- 목록 페이지에 필터/검색 기능이 없다. Phase 2에서 추가 시 클라이언트 vs 서버 어느 쪽이 적절한가?
- 타입 순서가 하드코딩(`typeOrder` 배열)되어 있다. 더 나은 방법이 있는가?
- `/archive/[id]`에서 존재하지 않는 id 접근 시 404 처리가 충분한가?
- 관련 문서(related_ids)가 편방향일 수 있다. 양방향 자동 추론이 필요한가?

---

## 질문 5: 코워크 스킬 설계 감사

`.claude/skills/strategy-archive-sync/` 디렉토리:

```
SKILL.md                              (67줄 — 트리거, Save/Load 모드, 타입 정의, 주의사항)
references/archive-schema.md          (87줄 — 디렉토리 구조, 타입 정의, JSON 스키마)
references/archive-entry-template.md  (85줄 — frontmatter 템플릿, 작성 가이드, 예시)
```

**SKILL.md의 Save 모드 절차:**
1. 대상 문서에서 메타데이터 추출
2. frontmatter YAML 작성
3. `documents/{type}/{year}/{date}-{slug}.md`에 파일 생성
4. `node scripts/rebuild-index.mjs` 실행
5. `npm run build` 빌드 확인
6. git commit + push

**SKILL.md의 Load 모드 절차:**
1. `index.generated.json` 읽기
2. 요청에 맞는 문서 필터링
3. md 파일 본문 읽어서 컨텍스트에 주입

점검 요청:
- Save 모드의 6단계 절차에 빠진 단계가 있는가? (예: id 중복 검사, 기존 문서 업데이트 vs 신규 생성 분기)
- Load 모드에서 토큰 예산 관리가 없다. 여러 문서를 로드할 때 컨텍스트 초과를 어떻게 방지하는가?
- references 파일 2개의 내용이 SKILL.md와 중복된다. 정보 구조를 개선할 수 있는가?
- 이 스킬이 코워크(Cowork mode)에서 자동 트리거되려면 어떤 조건이 필요한가?
- 스킬 실행 후 검증 단계(빌드 성공, 인덱스 정합성 등)가 충분한가?

---

## 질문 6: 현재 인덱스 데이터 정합성 전수 점검

현재 `index.generated.json`의 22개 문서 전체 목록입니다. 메타데이터의 정합성을 점검해 주십시오.

| # | id | type | date | version | canonical | tags 수 | token_hint | related_ids 수 |
|---|-----|------|------|---------|-----------|---------|------------|---------------|
| 1 | roadmap-2026-06-jun | roadmap | 2026-06-01 | v1 | true | 4 | medium | 1 |
| 2 | roadmap-2026-05-may | roadmap | 2026-05-01 | v1 | true | 4 | medium | 1 |
| 3 | roadmap-2026-04-apr | roadmap | 2026-04-15 | v1 | true | 4 | medium | 1 |
| 4 | roadmap-2026-03-mar | roadmap | 2026-03-15 | v1 | true | 4 | medium | 1 |
| 5 | audit-2026-04-07-archive-strategy | audit | 2026-04-07 | v1 | true | 4 | large | 2 |
| 6 | codex-prompt-...-v6-review-01-business-revenue | codex-prompt | 2026-04-07 | v6 | true | 4 | large | 1 |
| 7 | codex-prompt-...-v6-review-01b-retention-revenue | codex-prompt | 2026-04-07 | v6b | false | 4 | small | 1 |
| 8 | codex-prompt-...-v6-review-02-feasibility-resources | codex-prompt | 2026-04-07 | v6 | true | 4 | large | 1 |
| 9 | codex-prompt-...-v6-review-02b-agent-strategy | codex-prompt | 2026-04-07 | v6b | false | 4 | small | 1 |
| 10 | codex-prompt-...-v6-review-03-strategy-competition | codex-prompt | 2026-04-07 | v6 | true | 5 | large | 1 |
| 11 | codex-prompt-...-v6-review-03b-survey-dispute | codex-prompt | 2026-04-07 | v6b | false | 5 | small | 1 |
| 12 | codex-prompt-...-v9-1-review-01-checkin-coaching | codex-prompt | 2026-04-07 | v9.1 | true | 6 | large | 2 |
| 13 | codex-prompt-...-v9-1-review-02-revenue-kpi | codex-prompt | 2026-04-07 | v9.1 | true | 6 | large | 2 |
| 14 | codex-prompt-...-v9-1-review-03-agent-teacher | codex-prompt | 2026-04-07 | v9.1 | true | 6 | large | 2 |
| 15 | codex-review-...-01-ai-product-strategy | codex-review | 2026-04-07 | v1 | true | 4 | medium | 3 |
| 16 | codex-review-...-02-unit-economics | codex-review | 2026-04-07 | v1 | true | 4 | small | 3 |
| 17 | codex-review-...-03-edtech-retention | codex-review | 2026-04-07 | v1 | true | 4 | medium | 3 |
| 18 | codex-review-...-04-behavioral-economics | codex-review | 2026-04-07 | v1 | true | 3 | medium | 3 |
| 19 | codex-review-...-05-supply-side | codex-review | 2026-04-07 | v1 | true | 4 | medium | 3 |
| 20 | codex-review-...-06-payment-fintech | codex-review | 2026-04-07 | v1 | true | 4 | medium | 3 |
| 21 | codex-review-...-07-cross-analysis | codex-review | 2026-04-07 | v1 | true | 3 | small | 3 |
| 22 | data-analysis-...-escrow-gap | data-analysis | 2026-04-07 | v1 | true | 5 | large | 3 |

점검 요청:
- 22개 문서 전체가 canonical: true인데, 같은 주제의 구버전이 있는 경우 canonical 관리가 올바른가?
- codex-prompt의 v6과 v6b 구분이 적절한가? v6b(보조 관점)를 canonical: false로 설정한 근거가 타당한가?
- codex-prompt와 codex-review 사이의 related_ids가 정확한가? (프롬프트→자문 연결)
- tags 분포가 균형적인가? 특정 태그에 편중되거나 누락된 것이 있는가?
- id 네이밍이 일관적인가? (roadmap은 `2026-06-jun`, codex는 `2026-04-07-01-...` 형식 혼재)
- token_hint 값이 실제 파일 크기와 부합하는가?

---

## 질문 7: 태그 시스템 감사

현재 40개 태그의 전체 분포:

| 접두사 | 태그 수 | 주요 태그 |
|--------|---------|----------|
| topic/ | 24 | escrow(6), strategy(4), ai-agent(4), revenue(4), teacher(3), retention(3), checkin(3) |
| method/ | 3 | codex(16문서), cross-analysis(1), db-analysis(1) |
| metric/ | 3 | revenue(4), unit-economics(2), ltv(1) |
| period/ | 4 | may(4), apr(1), jun(1), mar(1) |
| artifact/ | 2 | review(6), prompt(4) |

점검 요청:
- method/codex가 16개 문서에 붙어있어 식별력이 낮다. 태그 세분화가 필요한가?
- topic/ 태그가 24개로 가장 많은데, 일부는 1개 문서에만 쓰인다(taxonomy, search, nudge 등). 최소 사용 빈도 기준이 필요한가?
- period/ 태그와 frontmatter의 period 필드가 중복이다. 하나로 통합하는 것이 나은가?
- artifact/ 태그와 artifact_kind 필드가 중복이다. 동일한 이유로 통합 가능한가?
- 태그 거버넌스: 새 태그를 추가할 때 기준/승인 프로세스가 필요한가?
- tags.generated.json이 태그→문서 단방향 매핑인데, 문서→태그 역매핑도 필요한가?

---

## 질문 8: 확장성 및 Phase 2 로드맵 감사

현재 Phase 1 완료 상태에서 Phase 2로 나아갈 방향을 평가해 주십시오.

**Phase 2 후보 작업:**
1. 나머지 문서 이관: 로드맵 v1~v9 이력, 코덱스 감사 A~E, SOP 4세트 12개 파일
2. UI 필터/검색: 타입별, 태그별, 기간별 필터 + 전문 검색
3. 관련 문서 자동 추천: 태그 유사도 기반
4. 아카이브 push 자동화: 코워크 스킬에서 자동 커밋/푸시
5. 컨텍스트 로딩 최적화: token_hint 기반 예산 관리

점검 요청:
- Phase 2 작업의 우선순위를 어떻게 매겨야 하는가?
- 문서가 50개, 100개, 200개로 늘어날 때 현재 구조(파일 기반 SSG)가 성능 병목이 되는가?
- 전문 검색을 추가할 때 인덱스에 본문 텍스트를 포함해야 하는가, 별도 검색 인덱스를 만들어야 하는가?
- 코워크 스킬의 자동 푸시가 안전한가? 실수로 잘못된 문서가 프로덕션에 반영될 위험은?
- 현재 시스템에 가장 큰 기술 부채(technical debt)는 무엇인가?

---

## 응답 형식

각 질문에 대해 아래 형식으로 응답해 주십시오:

1. **현재 상태 평가** (A/B/C/D/F 등급 + 1줄 요약)
2. **발견된 문제** (심각도 High/Medium/Low와 함께 목록)
3. **구체적 개선 제안** (우선순위 P1/P2/P3)
4. **Phase 2 권고사항** (해당 질문과 관련된 확장 방향)

마지막에 8개 질문의 전체 종합 평가와, 가장 시급하게 수정해야 할 Top 5 이슈를 정리해 주십시오.
